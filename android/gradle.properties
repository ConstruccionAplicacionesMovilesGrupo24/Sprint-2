org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Backend URLs are NOT configured here. Put overrides in android/local.properties (untracked),
# ~/.gradle/gradle.properties or environment variables. See android/README.md.
